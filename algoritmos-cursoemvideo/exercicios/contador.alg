algoritmo "conteate10"
var
 valor, salto, contador : inteiro
inicio
     contador <- 0
     Escreva("Quer contar ate quanto? ")
     Leia(valor)
     Escreva("Quantos sera o valor do salto? ")
     Leia(salto)
     Enquanto (contador <= valor)faca
            Escreval(contador)
            contador <- contador + salto
     fimEnquanto
     Escreval(" Terminei de contar ")
fimalgoritmo
